[build-system]
requires = ["maturin>=1.3,<2.0"]
build-backend = "maturin"

[project]
name = "dev_sandbox"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
# 不用 abi3：开发阶段直接绑定当前 Python，省去额外编译开销
features = ["pyo3/extension-module"]
module-name = "dev_sandbox"
