# RankIC 中性化计算方案独立设计报告

> 本报告为 `RANKIC_NEUTRALIZATION_TASK.md` 的独立交付物：所有结论由本任务从零推导，
> 未参考仓库中任何已有分析/交接/实验文档。实验代码与复现脚本位于
> `sandbox_rankic_neu/`（独立新建，未阅读 `sandbox/` 中既有实验代码）。

---

## 0. 摘要

| 问题 | 结论 |
|---|---|
| 备选思路（收益侧中性化一次、所有因子共享）数学上是否成立？ | **不成立（与现状 IC 不等价）**。逐日 IC 相关 0.97~0.99，max\|Δ\|≈0.1，符号一致率 91~94%；跨因子排序秩相关 0.93~0.98，top10 有 1/10 错位。 |
| 是否存在「收益侧只算一次 + 与现状 IC 完全一致」的方案？ | **字面意义上不存在**（数学原因：秩与投影不可交换，`ord(Ma)` 无法由共享量恢复）。但存在**精确等价 + 大幅共享**的方案 **C'**：省掉残差最终 rank + f32 cast，收益侧 `ord(r)` 一次共享，因子侧只保留一次投影与一次排序，**全量 30 因子 × 2276 天 × gap1/gap5 与现状逐位一致（max\|Δ\| = 0）**。 |
| 推荐方案 | 严格一致路线：C' + C''（预计算行业排序）+ C3-exact（无缺失因子快路径），**零阈值迁移**；可选：B 做预筛（端到端约 4 倍提速，需校准阈值/接受漏选风险）。 |

实测（生产引擎 ic_only 模式 40 因子）：标准中性化占单因子总耗时 **73.8~74.9%**，回测 IC 仅占 24.8%——与任务描述一致。

---

## 1. 问题形式化

### 1.1 现状链路（IC_A）的数学表述

以源码 `src/factor_neutralize_std.rs`（`neutralize_std_section`）与
`src/tail_v5_pipeline.rs`（`legacy_backtest_gap1_gap5_single_slot`、`legacy_spearman_correlation`）为准。
对每个 slot 的因子截面（省略日期下标，截面有 n 只股票）：

1. **rank pct**：`u = rank_pct(x)`（pandas `rank(axis=1, pct=True)` 语义：非 NaN 平均秩 / n_non_nan，NaN 保持；tie 按 f64 `==` 合并，含 -0.0/+0.0）；
2. **行业 OLS 填充** `F`：三级（申万二级 → 一级 → 全市场/有行业），每段用 size（barra value_2 的 rank pct）做 2 参数 OLS 填充因子 NaN。隐蔽副作用：**因子有值但 size 缺失的位置被覆盖为 NaN**（`c0 + c1·NaN = NaN`）；仅当日因子 unique ≥ 10、段内观测 ≥ 10 时执行；
3. **三级组中位填充**：median 固定来自步骤 2 后的矩阵（每级不重算），逐级填 NaN（末级只填行业码非 NaN 的股票）；
4. **restrict 置空**：`P_r`（restrict == 0 保留，否则 NaN）；
5. **再 rank pct**：`a = rank_pct(P_r F(u))`；
6. **OLS 残差**：`e = a − X β(a)`，`β(a) = (X'X)⁻¹X'a`。行业模式（生产默认）X = [10 风格, 申万一级 one-hot]（无显式截距，one-hot 行和为 1 隐含截距语义）；无行业模式 X = [1, 10 风格]。样本要求：当日有效数 n_v > 10；y 截面 unique == 1 时残差整体置 0.5；n_v ≤ 40 或 Cholesky 失败走 SVD 伪逆（真实数据 4000+ 走 Cholesky 快路径）；
7. **残差 rank pct**：`x_neu = rank_pct(e)`，f64 → f32 cast；
8. **回测**：信号 t−1 日、收益 t 日（`ret_sum_gap`，t 日起 gap 天累计），截面 = {x_neu 有限, ret 有限, restrict==0}，gap 日持有更新、gap 日取 IC：
   `IC_A = 1 − 6 Σ(ord(x_neu32) − ord(r32))² / (n(n²−1))`（ordinal 秩，tie 按索引稳定序给连续秩）。

### 1.2 关键记号

- `M = I − X(X'X)⁻¹X'`：风格投影的残差算子（对称幂等，`MX = 0`）；X 含常数向量时 `M·1 = 0`。
- `ord(·)`：ordinal 秩（tie 按索引）。
- `rank_pct(·)`：保序且 **tie 保持** 的变换：`x_i < x_j ⟺ r_i < r_j`，`x_i == x_j ⟺ r_i == r_j`。

---

## 2. 数学分析（推导链）

### 引理 1（残差最终 rank 冗余——方案 C 的理论基础）

**`IC_A = Spearman(e, r)`，其中 e 的秩在 f64 域计算。** 即步骤 7（残差 rank pct）与 f32 cast 对最终 IC **完全无影响**。

证明：
1. `x_neu = rank_pct(e)` 严格保序且 tie 保持，故 `ord(x_neu) = ord(e)`（tie 集合不变，tie 内按索引的稳定规则不变）。
2. f32 cast 不会引入新 tie：`rank_pct` 相邻值之差 ≥ 1/n_v ≈ 2e-4（n_v ≈ 5000），远大于 f32 在 (0,1] 上的绝对精度 ~6e-8，故 cast 严格保序、既有的 tie（`e_i == e_j`）保持。
3. 因此 `ord(x_neu32) = ord_f64(e)`。
4. Spearman 公式只依赖 `ord(x)` 与 `ord(r)` → `IC_A = Spearman_f64(e, r)`。∎

**注意（数值陷阱）**：若把 e 先 cast 成 f32 再求秩（方案 C 的朴素实现），cast 会把 |Δe| < f32 精度的相邻值合并成伪 tie，按索引序给秩，与 x_neu32 的秩不同——全量实测朴素版一致率仅 95%（差值 8e-7 级）。**必须在 f64 域对 e 求秩**（本报告 C' 的实现方式），则与现状 100% 逐位一致。

### 引理 2（协方差对称性）

若 `1 ∈ col(X)`（含截距；行业 one-hot 行和为 1 满足），对任意截面向量 a、b：
`cov(a, Mb) = cov(Ma, b) = cov(Ma, Mb)`。

证明：M 对称幂等且 `M1 = 0` ⟹ `mean(Mu) = 0`。`cov(a, Mb) = ⟨a − ā1, Mb⟩/n = ⟨a, Mb⟩/n = a'Mb/n = (Ma)'b/n = ⟨Ma, b⟩/n = cov(Ma, b)`（后一等号因 `mean(Ma)=0`）。同理 `b = Mb + Xγ`，`⟨Ma, Xγ⟩ = 0` ⟹ `cov(Ma, b) = cov(Ma, Mb)`。∎

**推论**：在 Pearson 线性世界，三种口径（只中性化因子 / 只中性化收益 / 双边中性化）分子相同。数值验证（真实截面，n≈2500）：三者相等至 3.5e-18（机器精度）。

### 命题 3（Spearman 破坏对称性——备选思路不等价的数学原因）

`ord(·)` 与 `M` **不可交换**：一般 `ord(Ma) ≠ M·ord(a)`（`ord` 非线性，M 线性）。故

- `IC_A = Pearson(ord(Ma), ord(r))` 与
- `IC_B = Pearson(ord(a), ord(Mb̃))`（b̃ 为收益侧链路的残差）

分子一般不等，IC_A ≠ IC_B。数值验证：真实截面 `corr(ord(Ma), M·ord(a)) = 0.9990 ≠ 1`；`cov(ord(Ma), ord(r)) = −0.0400` vs `cov(ord(a), ord(Mr̃)) = −0.0460`（该截面差 15%）。

**相等情形**（充要条件的充分方向）：
- (i) X = ∅（无中性化）：两边都退化为 `Spearman(a, r)`；
- (ii) `ord(a)` 与 `col(X)` 正交（几乎不可能）；
- (iii) `ord(r)` 与 `col(X)` 正交。

### 命题 4（「收益侧只算一次」的精确一致边界）

`IC_A` 需要 `ord(Ma)` 的**完整排序**：`Ma = a − X(X'X)⁻¹X'a` 依赖因子 a 的全体值与顺序，`ord(Ma)` 作为 a 的泛函无法由任何"仅依赖收益侧共享量"的表示恢复。因此：

**「因子侧零中性化计算」与「IC 精确一致」不可兼得。** 可达的精确一致最优 = 方案 C'（保留每因子一次投影 + 一次排序，其余全部共享/省去）。

### 引理 5（仿射抵消——C3 快路径的理论基础）

若 slot 无缺失且当日 size 无缺失，则现状链路的 `rank_pct → 填充 → rank_pct` 三步退化为**仿射变换**，OLS 残差对 y 的仿射变换保序，故 `IC_A = Spearman(OLS残差(x|_S), r)`——所有中间 rank 与填充均可跳过（C3-exact）。数值验证：残差秩仿射不变 ✓；C3-exact 与现状逐位一致 ✓。

若 size 有缺失（真实数据 barra 覆盖 ~57% 股票，size 缺失普遍），填充有真实副作用：**size 缺失位置的因子值被抹成 NaN 再被组中位填充**，填充值参与 rank 排序"挤秩"，改变有效股票的秩值——C3（跳过填充）不再精确，实测差异 max|Δ| ≈ 1e-4（corr 1.000000、符号一致率 100%）。

---

## 3. 数值验证（sandbox，全部不改主项目代码）

### 3.1 实验设置

- 数据：2276 天 × 5438 股模板（000905 回测输入 `000905_20170103_20260522_...`），
  风格 `/home/chenzongwei/database/barra/barra_daily_together_jason.parquet`，
  行业 `SW_IND_CODE.csv`（design_whatever 生产路径），真实因子取自 colblk 存储
  `/hdd/user_home_unsafe/chenzongwei/factor_store_挂单猫0701c`（30 个因子，缺失率 ~22%）。
- 因子预处理与生产一致：横截面平均秩 + 缺失填当日中位秩（_smooth_1 slot，无 NaN）。
- 基线 = 生产 `rp.neutralize_std_block_py` + 复刻 `legacy_spearman_correlation` 的回测对齐（held 信号、gap 持有、截面过滤逐一复刻）。
- sandbox 独立实现 `sandbox_rankic_neu/src/lib.rs`（pyo3 0.18 / numpy 0.18 / ndarray 0.15 / nalgebra 0.32，`bash build.sh` 安装为 `dev_sandbox_rankic`）。
- 复刻基线自证：sandbox 完整链路输出与生产 `neutralize_std_block_py` 输出 **逐位一致**（60 天样本 f32 bits 级；并全量 2276 天抽查 2 个真实因子，NaN 模式一致、有值处 max|diff| = 0）。

### 3.2 各方案与现状（A）的对照（30 因子 × 2276 天）

| 方案 | 定义 | gap | max\|Δ\| | 逐位一致率 | 时间序列 corr | 符号一致率 |
|---|---|---|---|---|---|---|
| **C'（精确等价）** | 省残差 rank_pct，resid 在 f64 域直接求秩 | 1 | **0** | **100%** | **1.000000** | **100%** |
| | | 5 | **0** | **100%** | **1.000000** | **100%** |
| **C3（近似）** | 跳过行业填充，restrict+rank+OLS 直接残差 | 1 | 9.1e-5 | 69.7% | 1.000000 | 100% |
| | | 5 | 5.7e-5 | 69.5% | 1.000000 | 100% |
| **B（备选思路）** | 收益侧中性化一次，因子侧零中性化 | 1 | 0.157 | 0% | 0.9713 | 94.1% |
| | | 5 | 0.103 | 0% | 0.9672 | 90.8% |

- C' 与 C3 的逐位一致率差异来自 f32 cast 伪 tie 与"挤秩"效应（见引理 1 注与引理 5）。
- B 的 IC 均值差无偏（mean(B−A) ≈ 0，std 0.004~0.012，个别因子 ±0.028）。
- **跨因子排序**：ρ(A, C3) = 1.0000（top10 重合 10/10）；ρ(A, B) = 0.98（gap1）/ 0.93（gap5），top10 重合 9/10。

### 3.2b 滚动 slot（含 NaN，覆盖填充路径）全量对照（30 因子 × 2276 天）

slot = 时间维滚动均值（window=20, min_periods=10，作用在秩填充后的因子上，前 9 天 NaN，
与生产派生 slot 形态一致，中性化走行业 OLS 填充 + 中位填充路径）：

| 方案 | gap | max\|Δ\| | 逐位一致率 | 时间序列 corr | 符号一致率 |
|---|---|---|---|---|---|
| C'（精确等价） | 1 / 5 | **0** | **100%** | **1.000000** | **100%** |
| C''（精确等价） | 1 / 5 | **0** | **100%** | **1.000000** | **100%** |
| C3-exact（快路径） | 1 / 5 | ~5e-5 | 85.5% | 1.000000 | ~100% |
| B（备选思路） | 1 / 5 | 0.09~0.12 | 0% | 0.977 / 0.967 | 93.5% / 87.3% |

结论：**C' 与 C'' 的逐位一致性在填充路径（有 NaN slot）同样成立**；C3-exact 对有 NaN 因子不再精确（快路径条件"因子无 NaN"不满足，差异 ~5e-5）；B 的不等价结论在两类 slot 上一致。

### 3.3 差异归因（A / B / 双边 F / raw，240 天 × 4 因子）

| | corr(A,B) | corr(A,F) | corr(A,raw) | corr(B,F) |
|---|---|---|---|---|
| gap1（4 因子范围） | 0.984~0.996 | **0.993~0.996** | 0.76~0.89 | 0.973~0.993 |
| gap5 | 0.979~0.989 | **0.986~0.994** | 0.81~0.93 | 0.957~0.992 |

**归因结论**：双边中性化 F 比单边收益中性化 B 更接近 A。A 与 B 的差异主要来自**因子侧**的秩替换（`ord(a)` 取代 `ord(Ma)`），而收益侧是否中性化影响很小——因为日度收益与风格横截面的相关性远弱于因子与风格的相关性。

### 3.4 B 的日期对齐敏感性

备选思路中收益侧中性化的风格暴露可对齐信号日（t−1，B1）或收益日（t，B2）：
corr(A,B1) = 0.978~0.996，corr(A,B2) = 0.971~0.994，corr(B1,B2) = 0.995~0.999。
对齐方式影响很小，**B ≠ A 的结论稳健**。

---

## 4. 性能

### 4.1 生产引擎实测占比（任务 2.3 的实测确认）

生产 `tail_backtest_engine` ic_only 模式、40 真实因子 × 13 slot、8 线程：

```
[PROF 完整回测 n=40 avg=105.7s] raw_cover=0.3% fold=0.0% rank_roll=0.0%
        bt_raw=0.0%  neutralize=74.9%  bt_neu=24.8%
```

**标准中性化占 74.9%**——与任务描述一致，是唯一大头。

### 4.2 中性化内部阶段分解（sandbox 阶段计时，2276×5438 单 slot）

| 阶段 | 秒 | 占比 | 是否与因子有关 |
|---|---|---|---|
| barra 10 风格 rank（预计算） | 9.2 | 56.4% | 无关（生产已共享） |
| OLS 残差（含 Cholesky） | 2.3 | 14.2% | 因子相关（nk 乘加不可省） |
| 三级组中位填充 | 1.7 | 10.7% | 因子相关（可预计算排序） |
| 因子 rank_pct（步骤 1） | 0.9 | 5.7% | 因子相关 |
| 残差 rank_pct（步骤 7） | 0.8 | 4.9% | **C' 省去** |
| restrict+rank（步骤 5） | 0.8 | 4.9% | 因子相关 |
| 行业 OLS 填充 | 0.5 | 3.2% | 因子相关（可预计算排序） |

### 4.3 方案相对提速（release 构建、共享预计算、5 次计时取 min/mean）

（"核心" = 扣除与因子无关的预计算：barra 风格 rank、行业码排序。sandbox 版含 barra 切片拷贝 1.23s，生产架构直接持有 10 个 (T,N) 矩阵、无此开销，故按核心成本对比。）

| 方案 | 单 slot 核心秒 | 相对现状核心 |
|---|---|---|
| 现状核心链路（rank1+fill+中位填充+rank+OLS+resid_rank） | 7.08 | 1.00× |
| C'（省残差 rank_pct + f32 cast，逐位一致） | 6.60 | 0.93×（省 ~7%，理论省 resid_rank 0.80s=11%） |
| C''（C' + 预计算行业码排序，逐位一致） | 5.93 | 0.84×（省 ~16%） |
| C3-exact（无 NaN 因子快路径，逐位一致） | 4.47 | 0.63×（省 ~37%） |
| B（收益侧一次，近似） | 0（仅回测 IC） | 端到端 ~4×（中性化 74.9% → 0） |

端到端（ic_only）估算：严格一致路线（C''+C3-exact）≈ 现状总时长的 0.85×（省 ~15%，
且零迁移成本）；B 预筛路线 ≈ 现状的 0.25× + 幸存者精确复核（预筛比例 10% 时总 ≈ 0.35×）。

### 4.4 两个额外探索的结论（如实记录）

1. **OLS valid 集合与因子无关（重要事实）**：对 15 个真实因子（缺失率 ~22%）全量 2276 天
   统计，每日 OLS 有效股票集合与"理想集合"（restrict==0 且 10 风格全部非 NaN）**100% 重合**，
   跨因子每日 distinct 集合数恒为 1。含义：设计矩阵 X、X'X 与 Cholesky 分解完全与因子无关，
   可在预计算阶段一次算好全局共享——这为 C3-exact 的快路径与生产 `neutralize_std_precompute`
   的进一步共享提供了事实基础。
2. **C⁗（Cholesky 分解缓存）探索：无提速，放弃**。基于上述事实实现"预计算每日 X'X 的
   Cholesky 因子 L、per-slot 仅做 X'y 累积 + L 回代"，与 C'' 逐位一致（验证通过），但实测
   per-slot 核心成本不降反升（+4%，X'X 累积并非瓶颈，且命中判定/矩阵组装引入固定开销）。
   结论：该方向不进入推荐方案，仅保留"valid 集合恒等"这一事实性发现。

---

## 5. 金融含义

- **IC_A**（现状）度量「因子在风格/行业空间正交化之后的秩」对原始未来收益秩的预测力。它是"因子挤干净后还剩多少 alpha"的标准口径，剔除的是因子侧的风格冗余。
- **IC_B**（备选）度量「因子原始秩」对「风格无法解释的收益残差秩」的相关。在 Pearson 线性世界它与 A 同分子（引理 2）；但 Spearman 的秩非线性使它偏向刻画"因子整体"而非"因子正交成分"，风格暴露重的因子在 B 口径下被系统性重估（实测个别因子 IC 偏移达 0.028）。
- **IC_F**（双边中性化）几乎等于 A（corr > 0.99）：收益侧中性化与否对结论影响甚微；中性化的价值集中在因子侧。
- **选因子语境的后果**：B 会重排候选（top10 错位 1/10），阈值 ic_point_neu_* 的统计意义改变，直接替换会改变入选集合。

---

## 6. 推荐方案与落地路径

### 6.1 取舍全景

| 方案 | 与现状一致性 | 单 slot 中性化成本 | 迁移影响 | 适用 |
|---|---|---|---|---|
| 现状 A | 基准 | 1.00× | — | — |
| **C'** | **逐位一致（max\|Δ\|=0）** | 0.93× | 零 | 全 slot |
| **C''** | **逐位一致** | 0.84× | 零 | 全 slot |
| **C3-exact** | **逐位一致（无 NaN 因子）** | 0.63× | 零 | _smooth_1 slot |
| C3（跳过填充） | 近似（max\|Δ\|~1e-4，corr 1.0000） | ~0.7× | 近似零 | 可接受近似时 |
| B（收益侧一次） | 不等价（corr 0.97~0.99） | 0（端到端 ~4×） | 阈值需重校准 | 预筛 |

### 6.2 推荐：严格一致路线（C'' + C3-exact），B 作为可选预筛

**第一步（零风险，必做）**：落地 C' —— 在 `neutralize_std_slot_f32` 链路中删除"残差 rank_pct + f32 cast"，回测的 `legacy_spearman` 对 f64 resid 求 ordinal 秩（收益侧 `ord(r)` 一次共享）。收益：省 ~7~11% 中性化成本（实测 7%、理论 11%），**结果与现状逐位一致，所有阈值、历史结论零迁移**。

**第二步（低风险，逐位一致）**：落地 C'' —— 把 `fill_ind_reg`/`fill_by_group_median` 的行业码排序移入 `neutralize_std_precompute`（与因子无关，一次性生成），fill 段内逻辑不变。收益：累计省 ~16%。

**第三步（低风险，逐位一致）**：落地 C3-exact —— 对无 NaN slot（_smooth_1，每因子 1 个）走快路径（抹 size-NaN + 组中位填充 + restrict + rank + OLS）。收益：该 slot 省 ~37%。

**第四步（可选，近似路线）**：B 预筛——收益侧中性化一次（r_neu 全因子共享），全因子快速 IC_B 排序，取 top-K（建议 K 保守放宽，如取并集口径 top 20~30%）的因子再走严格 C'' 复核。风险：B 与 A 的跨因子秩相关 0.93~0.98，存在漏选（top10 错位 1/10）；缓解 = 放宽预筛阈值 + 终筛仍用现状阈值与现状口径。适合因子量极大（万级）的场景。

### 6.3 对阈值/参数体系的迁移评估

- C'/C''/C3-exact：**结果逐位不变** → ic_point/ret_point/IR/覆盖率全部原样沿用，无任何迁移成本。
- B 预筛：预筛阶段使用自设的宽松阈值（与现状阈值无耦合）；终筛沿用现状阈值。判定器/入选集合由终筛（精确口径）决定，与现状一致；仅"预筛淘汰"的因子不被终筛评估——需要接受该漏选风险或提高 K。

### 6.4 落地代码位置

- `src/factor_neutralize_std.rs`：`neutralize_std_slot_f32` 增加"返回 f64 resid"变体；`neutralize_std_precompute` 增加行业码排序预计算。
- `src/tail_v5_pipeline.rs`：`process_v7_slot` 的中性化调用改用新变体；`legacy_backtest_single_factor_with_effective` 增加 resid64 入口（`ordinal_ranks` 的 f64 版本 + 混合 spearman）。
- 验证标准：与 `rp.neutralize_std_block_py` + 引擎 ic_neu_gap1/5 输出逐位对拍（本报告已给出 sandbox 版对拍方法，可直接复用为迁移后验收）。

---

## 7. 附录：实验设置与复现

- 全部实验脚本与独立 Rust 实现位于 `sandbox_rankic_neu/`：
  `prototype.py`（数据准备 + Python 复刻基线）、`experiment1.py`（小样本方案对照）、
  `full_experiment.py`（全量 30 因子 × _smooth_1 slot）、
  `full_experiment_rolling.py`（全量 30 因子 × 滚动 slot，含 NaN 填充路径）、
  `attribution.py`（差异归因）、`propositions.py`（数学命题数值验证）、
  `perf_test*.py`（提速基准）、`prod_engine_prof.py`（生产引擎耗时实测）、
  `src/lib.rs`（sandbox 实现，含 C'/C''/C3/C3-exact/C⁗/收益侧中性化/回测复刻）。
- 构建：`cd sandbox_rankic_neu && bash build.sh`（release：`maturin develop --release`）。
- 全量实验结论文件：`sandbox_rankic_neu/ic_compare_rows.npy`（_smooth_1）、
  `ic_compare_rows_rolling.npy`（滚动 slot）；生产全量抽查：`prod_fullspot.log`。
- 提速测量条件：release 构建（opt-level 3，无 LTO）、单线程、2276×5438 f32 输入、
  每方案 5 次计时取 min/mean；生产引擎实测为 8 线程 ic_only 模式。
